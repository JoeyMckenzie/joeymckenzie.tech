Follow Laravel Boost guidelines: stick to existing directory structure, use PHP 8 constructor property promotion, always declare explicit types, prefer PHPDoc for complex arrays, enums TitleCase. Use Form Requests for validation, prefer Eloquent relationships/model factories. Tailwind v4 CSS-first approach, use Flux UI components where possible. Run Pint before finalizing changes (vendor/bin/pint --dirty).